// General key/layout parameters
unit = 19.05;
stagger = 1/8;
offset = [
    -unit/2,
    -unit/2,
    0
];

// Cutout parameters
tunnel_height = 10; // height of the cutout (usually ~keycap height)
tunnel_wall_height = 8.5; // height of the wall added to the keycap (shorter than cutout so that it doesn't protrude above surface)
tunnel_d1 = 5; // bottom diameter
tunnel_d2 = 10; // top diameter

wall_thickness = 1.5;
